export default function Home() {
  return (
    <div className={""}>
      {/**
       * Initialize State controller/ scroll manager
       * states/scenes need to be passed through components
       * project cards should be held within
       * **/}
    </div>
  );
}
